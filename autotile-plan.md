# 오토타일 경계 보정 계획

## 현재 상태

아직 타일 경계 보정은 구현하지 않았다. 현재 렌더러는 각 셀을 사각형으로 가득 채우므로 서로 다른 바닥 타일이 만나는 지점과 코너가 직각으로 보인다.

이 문서는 구현 계획만 정의한다. 이번 작업에서는 렌더링 코드, 맵 데이터 형식과 이미지 자산을 변경하지 않는다.

## 목표

- 풀, 흙, 돌, 물 타일이 인접 셀의 종류에 따라 자연스럽게 이어져 보이게 한다.
- 직선 경계뿐 아니라 바깥쪽 둥근 코너와 안쪽 오목한 코너를 모두 보정한다.
- 대각선으로만 맞닿은 타일이 가느다란 선으로 잘못 연결되지 않게 한다.
- 셀 중심부의 지형 종류는 유지해 수작업 재현용 레이아웃이 달라지지 않게 한다.
- 저장 데이터에는 보정용 타일 번호를 기록하지 않고 원래의 바닥 타일 종류만 저장한다.

## 선택할 방식

8방향 이웃 비트마스크와 47개 형태로 정규화하는 Blob 오토타일 방식을 사용한다.

단순한 상·우·하·좌 4비트 16형만 사용하면 오목한 코너를 구분할 수 없다. 따라서 대각선 네 방향까지 검사하되, 대각선은 그 대각선에 접한 두 직교 방향도 연결된 경우에만 유효하게 취급한다. 이 규칙으로 불필요한 대각선 다리와 뾰족한 한 픽셀 연결을 막는다.

현재 바닥 무늬는 Canvas에서 절차적으로 그리므로 최초 구현은 별도의 타일 이미지 47장을 만들지 않는다. 재사용 가능한 경계 마스크를 Canvas Path2D로 정의하고 각 지형의 기존 색상과 무늬를 마스크 안에 그린다. 나중에 타일 아틀라스를 도입해도 동일한 마스크 번호를 그대로 사용할 수 있게 한다.

## 이웃 마스크 규칙

한 셀을 기준으로 다음 비트를 사용한다.

| 방향 | 비트 값 |
|---|---:|
| 위 N | 1 |
| 오른쪽 E | 2 |
| 아래 S | 4 |
| 왼쪽 W | 8 |
| 오른쪽 위 NE | 16 |
| 오른쪽 아래 SE | 32 |
| 왼쪽 아래 SW | 64 |
| 왼쪽 위 NW | 128 |

- 직교 이웃은 대상 지형과 같을 때 연결한다.
- 대각선 이웃은 대각선 셀과 그 양쪽 직교 셀이 모두 대상 지형일 때만 연결한다.
- 맵 바깥은 현재 셀과 같은 지형으로 간주해 문서 외곽이 불필요하게 둥글게 잘리지 않도록 한다.
- 원시 8비트 결과는 실제로 사용할 47개 표준 형태 ID로 정규화한다.
- 마스크 계산은 렌더링과 분리된 순수 함수로 작성한다.

## 서로 다른 지형의 겹침 규칙

경계에서 어느 지형이 위에 그려질지 매번 달라지지 않도록 시각적 우선순위를 둔다.

```text
풀 < 흙 < 돌 < 물
```

- 우선순위가 높은 지형이 낮은 지형 쪽으로 셀 크기의 최대 25%만 둥글게 확장된다.
- 셀 중심은 항상 해당 셀에 저장된 지형을 유지한다.
- 물은 개울 가장자리를 부드럽게 만들고, 돌은 흙이나 풀 위의 암반 경계를 표현한다.
- 우선순위는 화면 합성 규칙일 뿐 JSON 저장값이나 브러시 동작에는 영향을 주지 않는다.
- 실제 화면에서 물이 지나치게 넓어 보이면 지형별 확장 폭을 별도 상수로 조정한다.

## 코드 구성 계획

### 1. 순수 오토타일 계산 모듈

`apps/web/src/autotile.ts`를 추가한다.

- `getNeighborMask(map, column, row, ground)`로 8방향 연결 상태를 계산한다.
- `normalizeBlobMask(mask)`로 47개 표준 형태 ID를 반환한다.
- `getTransitionLayers(map, column, row)`로 현재 셀에 그릴 인접 지형과 형태, 우선순위를 계산한다.
- 렌더러나 DOM에 의존하지 않도록 해 단위 테스트가 가능하게 한다.

### 2. 경계 마스크 렌더러

`apps/web/src/main.ts`의 `drawGround`를 다음 순서로 분리한다.

1. 현재 셀의 기본 지형을 사각형으로 그린다.
2. 우선순위가 높은 인접 지형의 전이 레이어를 낮은 순서부터 그린다.
3. 표준 형태 ID에 대응하는 Path2D로 전이 영역을 클리핑한다.
4. 클리핑 영역 안에 기존 지형 무늬를 같은 좌표 기준으로 그린다.
5. 격자는 모든 지형 합성이 끝난 뒤 기존처럼 마지막에 그린다.

### 3. 편집 시 갱신 범위

- 현재 맵 크기에서는 기존 전체 Canvas 렌더링을 유지해 구현을 단순하게 한다.
- 향후 부분 렌더링을 도입할 때는 변경 셀과 주변 8개 셀만 무효화한다.
- 실행 취소, 다시 실행, JSON 불러오기와 초기화는 저장 형식이 바뀌지 않으므로 기존 흐름을 유지한다.

## 테스트 계획

- 동일한 지형으로 둘러싸인 셀이 완전 연결 형태를 반환하는지 검사한다.
- 상하좌우 직선, 바깥쪽 코너, 안쪽 코너와 한 칸 돌출 형태를 검사한다.
- 대각선만 같은 지형일 때 연결되지 않는지 검사한다.
- 맵 네 변과 네 모서리에서 범위를 벗어나지 않고 안정된 형태를 반환하는지 검사한다.
- 풀·흙·돌·물 조합에서 우선순위와 전이 레이어 순서가 항상 같은지 검사한다.
- 페인팅, 실행 취소, 다시 실행과 JSON 왕복 후 마스크 결과가 동일한지 검사한다.
- 대표 패턴을 작은 테스트 맵으로 렌더링해 직각 경계, 틈, 한 픽셀 다리가 없는지 데스크톱과 모바일에서 확인한다.

## 구현 순서

1. 비트마스크와 47형 정규화 함수 및 단위 테스트를 작성한다.
2. 실제 색 대신 반투명 디버그 색으로 모든 표준 형태를 한 화면에 표시한다.
3. Path2D 경계 마스크와 지형 우선순위를 기존 Canvas 렌더러에 연결한다.
4. 전이 폭과 코너 반지름을 개울이 있는 현재 샘플 맵에 맞춰 조정한다.
5. 정적 검사와 시각 검증을 통과한 뒤 Pages에 배포한다.

## 완료 조건

- 서로 다른 지형이 만나는 모든 직선과 코너에 눈에 띄는 직각 단절이 없다.
- 대각선 접촉만으로 지형이 연결되지 않는다.
- 브러시로 한 셀을 바꿨을 때 주변 경계가 즉시 일관되게 갱신된다.
- 기존 JSON을 수정 없이 불러올 수 있고 내보낸 JSON에도 파생 마스크 값이 추가되지 않는다.
- 오토타일 계산 단위 테스트, 기존 편집기 테스트, TypeScript 검사와 프로덕션 빌드가 모두 통과한다.
