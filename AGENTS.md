# 작업 기록 지침

- 사용자가 요청한 작업은 저장소 루트의 `requests.md`에 누적 기록한다.
- 각 작업은 `## 소제목 시작시간 ~ 종료시간 (소요시간)` 형식의 제목으로 작성한다.
- 시간은 한국 표준시(KST)를 기준으로 `YYYY-MM-DD HH:mm:ss` 형식을 사용한다.
- 소제목 아래에는 실제로 수행한 작업 내역을 간결하게 요약한다.
- 기존 기록은 수정하거나 덮어쓰지 않고 문서 끝에 새 기록을 추가한다.
- 사용자에게 묻는 질문은 작업 요청 기록과 섞지 않고 저장소 루트의 `questions.md`에 별도로 누적 기록한다.
- 질문 기록에는 질문한 시각, 질문 내용, 질문 이유를 적고, 답변을 받으면 답변 내용도 함께 추가한다.
- 질문하지 않은 작업은 `questions.md`에 항목을 만들지 않는다.

# 개발 및 배포 지침

- 이 프로젝트는 Cloudflare Pages 정적 프론트엔드, 별도 Cloudflare Worker API와 Cloudflare D1으로 구성한다.
- 현재 최초 페이지 단계에서는 Pages만 사용하고, D1과 API Worker는 이후 단계에서 추가한다.
- 배포는 Wrangler를 사용한다.
- 배포나 Cloudflare 리소스 구성을 변경하기 전에 저장소 루트의 `deployment-architecture.md`를 반드시 읽고 따른다.
- Pages, D1과 API Worker를 서로 독립된 관리·배포 지점으로 유지한다.
- Pages는 D1에 직접 접근하지 않고, 향후 별도 Worker API를 통해서만 D1 데이터를 사용한다.
- 작업 변경을 완료한 뒤 프로젝트에 정의된 정적 검사 명령을 실행한다.
- 정적 검사가 모두 성공한 경우에만 Wrangler로 배포한다.
- 배포까지 성공한 경우 해당 작업의 변경 사항을 커밋하고 현재 브랜치를 원격 저장소에 푸시한다.
- 정적 검사 또는 배포가 실패하면 커밋과 푸시를 진행하지 않고 실패 내용을 보고한다.
- Git 커밋 메시지는 영어로 작성한다.
- GitHub Actions와 `.github/workflows`를 사용하지 않는다.
- 정적 검사, D1 마이그레이션, Wrangler 배포, 커밋과 푸시는 권한이 설정된 로컬 환경에서 수행한다.
- 사람과 에이전트의 설정 책임 범위는 저장소 루트의 `setup-responsibilities.md`를 따른다.

# 데이터 및 시크릿 지침

- 애플리케이션의 영구 데이터 저장소는 Cloudflare D1을 사용한다.
- D1 스키마 변경은 마이그레이션 파일로 관리하고 로컬에서 검증한 뒤 원격 데이터베이스에 적용한다.
- API 키, 인증 토큰, 최초 커스텀 토큰 등 민감한 값은 가능한 한 Worker Secrets로 관리한다.
- 민감한 값을 `wrangler.jsonc`의 평문 `vars` 또는 D1에 저장하지 않는다.
- 로컬 개발용 시크릿은 `.dev.vars`로 관리하고 `.dev.vars*`가 Git에 커밋되지 않도록 한다.
- 실제 시크릿 값은 코드, 작업 기록, 질문 기록, 로그 또는 커밋에 남기지 않는다.
- 로컬 자동화와 배포 보조 스크립트는 PowerShell로 작성한다.
- 시크릿 키 구조는 버전 관리되는 `secrets.example.env`에 값 없이 정의한다.
- `secrets.example.env`의 각 항목에는 용도와 값을 어디서 획득하거나 어떻게 생성하는지 설명하는 주석을 작성하며, 해당 주석은 영어로 작성해도 된다.
- 실제 시크릿 값은 Git에서 제외된 `.dev.vars`에만 입력하며, 파일이 없으면 `scripts/Initialize-Secrets.ps1`로 생성한다.
- PowerShell 스크립트는 기존 시크릿 파일을 덮어쓰거나 시크릿 값을 콘솔에 출력하지 않는다.

# 맵 편집기 아키텍처 지침

- 맵은 셀 기반 2D 타일 방식으로 구현한다.
- 맵 데이터를 게임에서 직접 사용하지 않고, 게임 안에서 수작업으로 재구성할 때 참고하는 레이아웃 자료로 사용한다.
- 3D 메시, 높이, 경사, 버텍스 페인트와 공유 버텍스 동기화는 구현 범위에서 제외한다.
- 레이어, 좌표, 타일, 색상, 랜드마크, 메모와 참조 이미지 내보내기를 우선한다.
- 저장은 개별 셀이 아닌 청크 payload 단위로 D1에서 관리한다.
