# 맵 에디터 아키텍처 계획

## 현재 상태

- 단계: 2D 타일 방식 확정, 구현 전 세부 설계
- 확정 사항: 셀마다 타일과 메타데이터를 배치하는 2D 타일 방식으로 구현한다.
- 사용 목적: 저장 데이터를 게임에서 직접 불러오지 않고, 레이아웃을 참고해 게임 안에서 수작업으로 다시 제작한다.
- 제외 사항: 높이, 경사, 버텍스 페인트, 3D 메시 생성과 게임 런타임용 데이터 내보내기는 구현 범위에 포함하지 않는다.

## 제품 목표

- 빠르게 영역을 칠하고 지우며 전체 레이아웃을 구성한다.
- 레이어, 좌표, 색상, 타일, 마커와 메모를 이용해 수작업 재현에 필요한 정보를 남긴다.
- 확대·축소, 이동, 미니맵과 좌표 표시로 큰 맵을 쉽게 탐색한다.
- 참조용 이미지와 사람이 읽기 쉬운 맵 정보를 내보낼 수 있게 한다.
- 게임 엔진의 지형·오브젝트 형식과 결합하지 않는다.

## 선택지 비교

| 기준 | 2D 타일 배치 | 청크별 버텍스 데이터 | 공통 셀 + 선택적 버텍스 채널 |
|---|---|---|---|
| 기본 데이터 | 셀마다 타일 ID, 회전, 플래그 | 셀과 정점을 함께 관리 | 셀은 항상 존재하고 버텍스는 필요한 레이어만 보유 |
| 구현 난이도 | 낮음 | 높음 | 초기에는 낮고 기능 확장 시 증가 |
| 페인팅 | 셀 단위로 단순함 | 브러시 영향 정점과 인접 청크 동기화 필요 | 셀 페인팅부터 구현하고 버텍스 브러시는 별도 추가 |
| 높이와 경사 | 표현하기 어려움 | 자연스럽게 표현 가능 | 필요할 때만 지원 가능 |
| 지형 경계 혼합 | 오토타일 규칙 필요 | 버텍스 가중치로 부드러운 혼합 가능 | 타일 규칙과 버텍스 혼합을 선택 가능 |
| 저장과 네트워크 | 비교적 작고 단순함 | 데이터량과 수정 범위가 커짐 | 사용하지 않는 채널은 저장하지 않음 |
| 청크 경계 | 타일 이웃 규칙만 처리 | 공유 정점의 이음새 관리가 핵심 | 버텍스 채널 사용 시에만 이음새 처리 |
| 적합한 결과물 | 평면 2D, 픽셀·타일 기반 게임 | 높낮이가 있는 3D 지형 | 에디터 확장 가능성을 남겨야 하는 경우 |

## 선택 결과

최종 결과를 게임에서 직접 사용하지 않고 레이아웃 참고 자료로 사용하므로 2D 타일 방식을 선택한다. 버텍스 방식의 높이, 경사와 재질 혼합 기능보다 타일 페인팅 속도, 좌표 확인, 메모와 레이어 관리가 더 중요하다.

## 권장 데이터 구조

렌더러가 아니라 논리 좌표와 청크를 저장의 기준으로 삼는다.

```text
Map
└─ Layer
   └─ Chunk (chunkX, chunkY)
      └─ Cells[chunkSize × chunkSize]
         ├─ tileId
         ├─ rotation
         ├─ flags
         ├─ color 또는 variant
         └─ properties
```

- 초기 청크 크기는 `32 × 32` 셀을 후보로 두되 구현 후 `16`, `32`, `64` 크기를 비교 측정한다.
- 셀 데이터에 포맷 버전을 두어 향후 타일 속성을 안전하게 확장한다.
- 청크 경계에서는 오토타일 또는 이웃 셀 규칙만 처리하며 공유 버텍스 동기화는 하지 않는다.
- 랜드마크, 수작업 배치 대상, 안내 메모처럼 셀 배열과 수명이 다른 데이터는 별도 엔티티로 저장한다.

## D1 저장 계획

셀 하나마다 D1 행을 만들지 않고 청크를 저장 단위로 사용한다.

| 테이블 | 역할 |
|---|---|
| `maps` | 맵 메타데이터, 좌표계, 기본 청크 크기, 전체 revision |
| `map_layers` | 타일, 지형, 충돌, 오브젝트 등 레이어 정의와 순서 |
| `map_chunks` | 맵·레이어·청크 좌표별 payload, 포맷 버전, revision |
| `map_objects` | 독립 오브젝트, 스폰 지점, 트리거와 속성 |
| `map_revisions` | 저장 이력과 향후 되돌리기·감사를 위한 메타데이터 |

`map_chunks`는 `(map_id, layer_id, chunk_x, chunk_y)`를 복합 키로 사용한다. 초기에는 디버깅하기 쉬운 JSON payload로 시작하고, 실제 크기와 처리 속도를 측정한 뒤 필요할 때 BLOB 인코딩을 도입한다. D1은 SQLite 계열 JSON 기능과 BLOB 바인딩을 지원하므로 두 형식을 모두 선택할 수 있다.

## 편집 및 저장 흐름

1. 클라이언트는 화면에 필요한 청크만 불러온다.
2. 브러시 입력은 먼저 클라이언트 메모리의 셀 배열에 적용한다.
3. 변경된 청크만 dirty 상태로 표시한다.
4. 마우스 입력마다 서버에 쓰지 않고 일정 시간 단위 또는 명시적 저장 시 dirty 청크를 묶어 전송한다.
5. 서버는 `baseRevision`을 검사하고 충돌이 없을 때 새 payload와 revision을 D1에 저장한다.
6. 충돌이 있으면 덮어쓰지 않고 클라이언트에 충돌 결과를 반환한다.

## 구현 단계

### 1단계: 데이터 모델 검증

- 셀, 레이어, 청크 좌표 변환 규칙을 순수 TypeScript로 정의한다.
- 타일 청크의 직렬화와 역직렬화 예제를 만든다.
- 음수 좌표, 청크 경계, revision 충돌 테스트를 먼저 작성한다.

### 2단계: 2D 타일 MVP

- 맵 생성, 확대·축소, 이동, 셀 선택, 브러시, 지우개를 구현한다.
- 레이어, undo/redo, dirty 청크 추적을 구현한다.
- 렌더링과 저장 모델을 분리해 이후 렌더러 교체가 가능하게 한다.

### 3단계: Workers와 D1 저장

- 맵 메타데이터 및 청크 조회·저장 API를 구현한다.
- D1 마이그레이션과 로컬 테스트를 작성한다.
- 여러 dirty 청크를 한 번에 저장하고 revision 충돌을 검증한다.

### 4단계: 수작업 재현 지원 기능

- 좌표 눈금, 미니맵, 랜드마크와 메모 기능을 구현한다.
- 전체 맵 또는 선택 영역을 참조 이미지로 내보내는 기능을 구현한다.
- 타일 범례와 레이어 정보를 사람이 읽기 쉬운 형태로 함께 제공한다.

### 5단계: 배포 준비

- 정적 검사, 테스트, 로컬 D1 마이그레이션을 통과시킨다.
- PowerShell 배포 스크립트에서 원격 마이그레이션과 Wrangler 배포를 수행한다.
- 배포 확인 후 영어 메시지로 커밋하고 현재 브랜치를 푸시한다.

## 이번 단계에서 구현하지 않는 항목

- 실제 렌더러와 편집 UI
- D1 데이터베이스와 테이블 생성
- 운영 배포
- 3D 메시, 높이맵과 버텍스 페인트
- 게임 런타임에서 직접 사용하는 데이터 형식과 엔진 전용 내보내기

## 참고 문서

- Cloudflare D1: https://developers.cloudflare.com/d1/
- D1 Workers Binding API: https://developers.cloudflare.com/d1/worker-api/
- D1 JSON 쿼리: https://developers.cloudflare.com/d1/sql-api/query-json/
