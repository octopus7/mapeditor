# 질문 기록

사용자에게 확인 질문을 한 경우에만 질문한 시각, 질문 내용, 질문 이유를 기록하고 답변을 받으면 해당 항목에 답변을 추가한다.

## D1 및 Worker Secrets 설정 가능 범위

- 질문 시각: 2026-08-02 15:27:28 KST
- 질문 내용: D1을 저장소로 사용하고 민감한 값은 Worker Secrets로 처리할 때, 최초 커스텀 토큰을 포함해 Codex가 어디까지 설정할 수 있는가?
- 질문 이유: 프로젝트 초기 구성에서 자동으로 처리할 수 있는 작업과 사용자가 직접 입력해야 하는 비밀값의 경계를 확인하기 위해 질문했다.
- 답변: Codex는 Workers 프로젝트 구성, D1 생성 및 바인딩 설정, 스키마와 마이그레이션 작성·적용, Worker의 D1 접근 코드, 필수 시크릿 이름 선언, 로컬 시크릿 파일 제외 설정, 정적 검사와 배포까지 처리할 수 있다. 실제 시크릿 값은 노출 방지를 위해 사용자가 Wrangler의 대화형 명령이나 Cloudflare 대시보드에서 직접 입력하는 것이 가장 안전하다. 최초 커스텀 토큰 역시 Worker Secret으로 한 번 주입하고, 코드에서는 해당 바인딩 이름만 참조하도록 구성할 수 있다.

## PowerShell에서 별도 시크릿 파일 참조 가능 여부

- 질문 시각: 2026-08-02 15:32:04 KST
- 질문 내용: 에이전트가 PowerShell 스크립트를 만들고, 민감한 값은 버전 관리되는 예시 파일을 참고해 Git에서 제외된 별도 파일에 사용자가 입력하며, 실제 파일이 없으면 자동 생성하는 구조가 가능한가?
- 질문 이유: 자동화 편의성을 유지하면서 실제 시크릿 값이 저장소와 Git 이력에 포함되지 않도록 구성할 수 있는지 확인하기 위해 질문했다.
- 답변: 가능하다. `secrets.example.env`에는 키와 획득 방법만 두고, `scripts/Initialize-Secrets.ps1`이 실제 입력 파일 `.dev.vars`가 없을 때만 복사하도록 구현했다. `.dev.vars`는 `.gitignore`로 제외하며 스크립트는 기존 파일을 덮어쓰거나 내용을 출력하지 않는다.
