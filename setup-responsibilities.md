# 설정 책임 구분

## 사람이 반드시 직접 해야 하는 항목

- [ ] Cloudflare 계정을 생성하고 결제 수단, 요금제, 약관과 계정 소유권을 확인한다.
- [ ] 최초 Wrangler 로그인 과정에서 브라우저 인증, SSO 또는 MFA 승인을 완료한다.
- [ ] 에이전트가 사용할 Cloudflare 및 Git 권한을 최소 범위로 부여한다.
- [ ] `scripts/Initialize-Secrets.ps1`이 생성한 `.dev.vars`에 `BOOTSTRAP_TOKEN`을 포함한 실제 시크릿 값을 직접 입력한다.
- [ ] 외부 도메인 등록기관이나 제3자 서비스에서 요구하는 본인 확인과 결제 승인을 직접 처리한다.
- [ ] 운영 데이터 삭제, D1 데이터베이스 삭제, 시크릿 삭제 등 복구가 어렵거나 파괴적인 작업을 명시적으로 승인한다.

실제 시크릿 값과 Cloudflare API 토큰은 대화, 코드, 문서, 로그 또는 Git 이력에 남기지 않는다.

## 권한 확보 후 에이전트가 수행할 수 있는 영역

| 영역 | 에이전트가 수행할 수 있는 작업 | 필요한 선행 조건 |
|---|---|---|
| Workers 프로젝트 | TypeScript 프로젝트 구성, `wrangler.jsonc` 관리, 바인딩과 환경 설정, 타입 생성 | 저장소 쓰기 권한 |
| D1 | 데이터베이스 생성, 바인딩 연결, 스키마 및 마이그레이션 작성, 로컬 검증, 원격 적용, 쿼리 코드 작성 | D1 관리 권한과 Wrangler 인증 |
| Worker Secrets | 예시 파일과 PowerShell 초기화·등록 스크립트 작성, 필요한 시크릿 이름 선언, 타입과 참조 코드 작성, 등록된 시크릿 이름 확인 | 실제 값은 사람이 `.dev.vars`에 입력 |
| 검증 | 린트, 타입 검사, 테스트, Wrangler 배포 사전 검사, 로컬 D1 마이그레이션 검증 | 프로젝트 의존성 설치 가능 |
| 배포 | 원격 D1 마이그레이션 적용, Wrangler 배포, 배포 결과와 로그 확인 | Workers 배포 및 D1 쓰기 권한 |
| Git | 영어 메시지로 작업 변경 커밋, 현재 브랜치 푸시 | Git 원격 인증과 푸시 권한 |
| 도메인 | Worker 라우트와 Cloudflare DNS 설정 | 대상 Zone의 필요한 권한과 사용자의 도메인 결정 |

## 사람이 값을 입력하고 에이전트가 연결하는 항목

- 에이전트는 값이 비어 있는 `secrets.example.env`와 PowerShell 스크립트를 작성하고 버전 관리한다.
- 예시 파일의 각 시크릿 항목에는 용도와 값을 어디서 획득하거나 어떻게 생성하는지 안내하는 주석을 포함한다.
- PowerShell 초기화 스크립트는 `.dev.vars`가 없을 때만 예시 파일을 복사하며 기존 파일은 덮어쓰지 않는다.
- `BOOTSTRAP_TOKEN` 같은 최초 관리자 토큰은 사람이 Git에서 제외된 `.dev.vars`에 직접 입력한다.
- 이후 배포 스크립트는 `.dev.vars`를 참조해 Worker Secrets를 등록하되 파일 내용은 출력하지 않는다.
- 에이전트는 시크릿의 실제 값을 읽거나 기록하지 않고 Worker 코드에서 시크릿 바인딩 이름만 참조한다.
- 토큰 교체가 필요하면 에이전트가 영향 범위와 절차를 준비하고, 새 값 등록은 사람이 직접 수행한다.
- 사용자가 계속 발급하는 세션 또는 API 토큰은 원문을 D1에 저장하지 않고 검증에 필요한 해시와 메타데이터만 저장하도록 구현한다.
- Wrangler 배포용 Cloudflare 인증 정보와 애플리케이션의 `BOOTSTRAP_TOKEN`은 서로 분리한다.

## 로컬 실행 및 배포 방식

- GitHub Actions와 `.github/workflows`는 사용하지 않는다.
- 개발, 정적 검사, D1 마이그레이션, 배포, 커밋과 푸시는 권한이 설정된 로컬 환경에서 수행한다.
- 로컬 및 배포 자동화 스크립트는 PowerShell로 작성한다.
- 실행 순서는 `로컬 검증 → 정적 검사 → 원격 D1 마이그레이션 → Wrangler 배포 → 배포 확인 → 커밋 → 푸시`를 기본으로 한다.
- 정적 검사, 원격 마이그레이션 또는 배포가 실패하면 커밋과 푸시를 진행하지 않는다.

## 에이전트가 임의로 수행하지 않는 작업

- 실제 시크릿 값 생성·노출·문서화
- 결제 수단, 요금제 또는 계정 소유권 변경
- 명시적 승인 없는 운영 데이터 및 Cloudflare 리소스 삭제
- 요청 범위를 벗어난 Cloudflare 계정·Zone 설정 변경
- GitHub Actions 또는 다른 자동 배포 파이프라인 추가
